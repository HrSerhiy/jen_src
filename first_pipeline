pipeline {
    agent any

    stages {
        stage('Hello1') {
            steps {
                sh 'ssh sa@localhost'
                sh 'ssh sa@localhost'
                sh 'echo \'Hello World\''
            }
        }
    }
}
